import Link from "next/link";import { demoExperience } from "@/features/demo/repository";
export default function DemoRoute(){return <><header className="demo-page-hero"><span className="demo-badge">Vista de demostración</span><span className="eyebrow">Siete días contigo</span><h1>Tu ruta</h1><p>Un recorrido breve para volver a elegirte con claridad.</p></header><div className="cinematic-route">{demoExperience.days.map(([number,title,description,open])=><section className={open?"route-chapter open":"route-chapter"} key={number}><span>{String(number).padStart(2,"0")}</span><div><h2>{title}</h2><p>{description}</p></div>{open?<Link href="/demo/dia/1">Abrir</Link>:<small>Próximamente</small>}</section>)}</div></>}
