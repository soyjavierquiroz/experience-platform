import { DemoDayOne } from "@/components/demo-day-one";
import { loadContent } from "@/features/content/loader";
export default async function DemoDayOnePage(){const content=(await loadContent()).find(entry=>entry.tenant.slug==="mnle");const videoAsset=content?.assets.find(asset=>asset.key==="mnle-dia-1-video");if(!videoAsset)throw new Error("No se encontró el video del Día 1");return <><header className="demo-page-hero"><span className="demo-badge">Vista de demostración</span><span className="eyebrow">Día 1 de 7</span><h1>Reconoce el ciclo</h1><p>Observa qué ocurre antes, durante y después del impulso de escribirle.</p></header><DemoDayOne videoAsset={videoAsset}/></>}
