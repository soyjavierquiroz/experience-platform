import type { ContentAsset,ContentBlock } from "./schema";import { VideoPlayer } from "./video";
export function ContentBlockRenderer({block,assets={}}:{block:ContentBlock;assets?:Record<string,ContentAsset>}){switch(block.type){case "heading":return block.level===2?<h2>{block.text}</h2>:<h3>{block.text}</h3>;case "richText":return <div className="prose-copy">{block.paragraphs.map((text,i)=><p key={i}>{text}</p>)}</div>;case "callout":return <aside className={`callout ${block.tone}`}><strong>{block.title}</strong><p>{block.text}</p></aside>;case "video":{const asset=assets[block.assetRef];return asset?<VideoPlayer asset={asset} title={block.title}/>:<section className="video-fallback" role="status">Video pendiente de configuración</section>}case "audio":return <section className="soft-card"><span className="eyebrow">Audio</span><h2>{block.title}</h2><p>Disponible próximamente.</p></section>;case "image":return <section className="soft-card"><h2>{block.title}</h2><p>{block.alt} · Imagen pendiente</p></section>;case "download":return <section className="soft-card resource"><div><h2>{block.title}</h2><p>{block.description}</p></div><button className="button secondary" disabled>Próximamente</button></section>;case "reflection":return <section className="reflection"><h2>{block.prompt}</h2></section>;case "textInput":return <label>{block.label}<input required={block.required}/></label>;case "textarea":return <label>{block.label}<textarea required={block.required}/></label>;case "singleChoice":case "multiChoice":return <fieldset><legend>{block.label}</legend>{block.options.map(o=><label key={o.value}><input type={block.type==="singleChoice"?"radio":"checkbox"}/>{o.label}</label>)}</fieldset>;case "scale":return <label>{block.label}<input type="range" min={block.min} max={block.max}/></label>;case "checklist":return <section className="soft-card"><h2>{block.title}</h2>{block.items.map(i=><label key={i.id}><input type="checkbox"/> {i.label}</label>)}</section>;case "timer":return <section className="soft-card"><h2>{block.title}</h2><p>{Math.round(block.durationSeconds/60)} minutos</p></section>;case "action":return <section className="action-card"><h2>{block.title}</h2><p>{block.description}</p></section>;case "divider":return <hr/>;case "completion":return <button className="button">{block.label}</button>}}
