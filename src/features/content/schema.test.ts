import {describe,expect,it} from "vitest";import {aspectRatioSchema,contentAssetSchema,contentBlockSchema,moduleContentDocumentSchema} from "./schema";import {normalizeYouTubeMediaId} from "./video";
describe("Content Engine schema",()=>{it("acepta bloques versionados",()=>expect(contentBlockSchema.parse({id:"day-video",type:"video",version:1,title:"Video",assetRef:"video-demo"}).type).toBe("video"));it("rechaza iframe arbitrario",()=>expect(contentBlockSchema.safeParse({id:"x",type:"video",version:1,title:"x",iframe:"<iframe/>"}).success).toBe(false));it("custom exige dimensiones",()=>expect(aspectRatioSchema.safeParse({type:"custom"}).success).toBe(false));it("3:4 es válido",()=>expect(aspectRatioSchema.parse({type:"3:4"}).type).toBe("3:4"));it("documento inválido falla",()=>expect(moduleContentDocumentSchema.safeParse({schemaVersion:"1.0"}).success).toBe(false));it("Bunny ready requiere metadata",()=>expect(contentAssetSchema.safeParse({schemaVersion:"1.0",key:"bunny-video",tenantSlug:"mnle",category:"video",provider:"bunny",providerMetadata:{},title:"x",aspectRatio:{type:"3:4"},status:"ready"}).success).toBe(false))});
describe("YouTube",()=>{it("acepta ID",()=>expect(normalizeYouTubeMediaId("M7lc1UVf-VE")).toBe("M7lc1UVf-VE"));it("normaliza URL",()=>expect(normalizeYouTubeMediaId("https://youtu.be/M7lc1UVf-VE")).toBe("M7lc1UVf-VE"));it("rechaza URL desconocida",()=>expect(()=>normalizeYouTubeMediaId("https://example.com/video")).toThrow())});
