import {readFileSync} from "node:fs";import {renderToStaticMarkup} from "react-dom/server";import {describe,expect,it} from "vitest";import type {ContentAsset} from "@/features/content/schema";import {SmartVideoPlayer} from "./SmartVideoPlayer";
const creator={displayName:"Creator",role:"Guide"};
function asset(type:"9:16"|"16:9"|"3:4"|"4:5"):ContentAsset{return {schemaVersion:"1.0",key:`video-${type.replace(":","-")}`,tenantSlug:"tenant",category:"video",provider:"youtube",providerMediaId:"KD4sVeHm1gg",providerMetadata:{},title:"Creator video",aspectRatio:{type},status:"published"}}
describe("SmartVideoPlayer aspect ratios",()=>{it.each(["9:16","16:9","3:4","4:5"] as const)("derives %s from the asset",type=>{const html=renderToStaticMarkup(<SmartVideoPlayer asset={asset(type)} creator={creator} context="test"/>);expect(html).toContain(`aspect-ratio:${type.replace(":"," / ")}`);expect(html).not.toContain("iframe")});it("keeps fullscreen available in native fallback",()=>{const source=readFileSync(new URL("./SmartVideoPlayer.tsx",import.meta.url),"utf8");expect(source).toContain("allowFullScreen");expect(source).toContain("requestFullscreen")})});
