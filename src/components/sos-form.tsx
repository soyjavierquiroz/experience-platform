"use client";
import { useState } from "react";
export function SosForm(){const [state,setState]=useState("idle");const [seconds,setSeconds]=useState(600);const [running,setRunning]=useState(false);function start(){setRunning(true);const timer=window.setInterval(()=>setSeconds(value=>{if(value<=1){window.clearInterval(timer);setRunning(false);return 0}return value-1}),1000)}async function submit(form:FormData,completed:boolean){setState("saving");const response=await fetch("/api/sos",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({messageText:form.get("messageText"),expectedResponse:form.get("expectedResponse"),desiredOutcome:form.get("desiredOutcome"),emotionBefore:Number(form.get("emotionBefore")),emotionAfter:form.get("emotionAfter")?Number(form.get("emotionAfter")):undefined,completed})});setState(response.ok?(completed?"completed":"saved"):"error")}
return <form className="content-flow" action={form=>void submit(form,false)}><section className="reflection"><label>¿Qué intensidad tiene la emoción ahora? <input name="emotionBefore" type="range" min="1" max="10" defaultValue="5"/></label><label>¿Qué quieres escribirle?<textarea name="messageText" required maxLength={5000}/></label><label>¿Qué esperas recibir al enviarlo?<textarea name="expectedResponse" required maxLength={2000}/></label><label>¿Qué estás buscando?<select name="desiredOutcome" defaultValue="claridad"><option value="claridad">Claridad</option><option value="atencion">Atención</option><option value="reconciliacion">Reconciliación</option><option value="respuesta">Respuesta</option><option value="alivio">Alivio</option><option value="otra">Otra</option></select></label><button className="button" type="submit">Guardar sin enviar</button></section><section className="soft-card"><h2>Pausa de diez minutos</h2><p className="timer">{Math.floor(seconds/60)}:{String(seconds%60).padStart(2,"0")}</p><button className="button secondary" type="button" disabled={running||seconds===0} onClick={start}>{running?"Pausa en curso":"Iniciar pausa"}</button></section><aside className="callout calm"><strong>Frase ancla</strong><p>No necesito resolverlo en este instante. Puedo volver a mí antes de elegir.</p></aside><section className="reflection"><label>¿Qué intensidad tiene la emoción después?<input name="emotionAfter" type="range" min="1" max="10" defaultValue="5"/></label><button className="button" type="button" onClick={event=>void submit(new FormData(event.currentTarget.form!),true)}>Finalizar esta pausa</button><p aria-live="polite">{state==="saved"?"Guardado en tu espacio privado.":state==="completed"?"Pausa finalizada y guardada.":state==="error"?"No se pudo guardar. Tu texto sigue en esta pantalla.":""}</p></section></form>}
