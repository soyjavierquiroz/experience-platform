import { Brand } from "@/components/brand"; import type { TenantConfig } from "@/features/tenants/types"; import { BottomNav } from "./bottom-nav";
export function AppShell({tenant,email,children}:{tenant:TenantConfig;email:string;children:React.ReactNode}){return <div className="app-shell"><header className="app-header"><div className="brand-name"><Brand tenant={tenant}/></div><div className="avatar" aria-label={`Sesión de ${email}`}>{email.slice(0,2).toUpperCase()}</div></header><main className="main">{children}</main><BottomNav/></div>}
